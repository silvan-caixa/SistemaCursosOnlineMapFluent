# dotnet run (normal)
# dotnet run --no-build (rapido)
# dotnet watch run (mais rapido)
# Modo Release: Isso roda sem build, sem overhead — ideal para testar performance.
```dotnet publish -c Release
```./bin/Release/net8.0/<seu_executavel>

